name: wealthassure_app
description: Wealth Assure Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_svg: ^2.0.10   # <-- add this

flutter:
  uses-material-design: true
  assets:
    - assets/logo.svg    # <-- add this line
